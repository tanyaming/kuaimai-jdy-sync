{
  "name": "kuaimai-jdy-sync",
  "version": "2.0.0",
  "description": "快麦ERP → 简道云 子订单同步服务",
  "main": "dist/sync.js",
  "scripts": {
    "sync": "tsx src/sync.ts",
    "sync:once": "tsx src/sync.ts --once",
    "sync:full": "tsx src/sync.ts --full",
    "build": "tsc",
    "start": "node dist/sync.js"
  },
  "dependencies": {
    "axios": "^1.7.0",
    "dotenv": "^16.4.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.7.0",
    "typescript": "^5.4.0"
  }
}
